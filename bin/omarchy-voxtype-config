#!/bin/bash

# omarchy:summary=Open the Voxtype configuration file

set -e

# Used by the Voxtype shell bar widget to open config on right click

exec omarchy-launch-editor ~/.config/voxtype/config.toml
