#!/bin/bash

# Adjusts display brightness and shows OSD

if [[ -z "$1" ]]; then
  echo "Usage: omarchy-brightness-display <+5%|5%->..."
  exit 1
fi

brightnessctl set "$1" -q
omarchy-swayosd-brightness
