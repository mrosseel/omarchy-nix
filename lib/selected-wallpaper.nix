config: {
  # Wallpaper path points to the symlink that tracks current background
  # omarchy-bg-next manages this symlink and cycles through theme-specific wallpapers
  wallpaper_path = "~/.config/omarchy/current/background";
}
